# おまけ: pnpmのインストール

Diskernel の使用には pnpm をおすすめします（Diskernel は pnpm を前提として開発されているライブラリです）。

npm が利用できる場合、以下のコマンドでインストールできます。

```bash
npx pnpm@latest dlx @pnpm/exe@latest setup
source ~/.bashrc  # または任意のシェルの rc ファイル
```

詳しくは [pnpm の公式ドキュメント](https://pnpm.io/ja/installation) を参照してください。
